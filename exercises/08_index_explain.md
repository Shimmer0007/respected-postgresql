# 第 08 章练习：表设计与索引

1. 在事务中尝试插入重复玩家名，并观察 `UNIQUE` 约束报错；最后回滚。
2. 在事务中尝试插入数量为 0 的采集记录，并观察 `CHECK` 约束报错；最后回滚。
3. 阅读“按玩家和时间筛选”的 `EXPLAIN (ANALYZE, BUFFERS)`，指出扫描方式和实际返回行数。
4. 设计 `server_announcements` 表，要求标题非空、作者必须存在、标签可以为空数组。
5. 挑战：为“按状态查找未完成建筑”提出一个索引，并说明写入成本。
