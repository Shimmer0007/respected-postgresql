# 第 02 章练习：筛选稀有资源

1. 找出发生在下界（`dimension_id = 2`）且数量至少为 10 的采集事件。
2. 找出坐标高度 `y` 小于 0 的事件，并按 `y` 从低到高排序。
3. 找出方块稀有度为 `rare` 或 `epic` 的方块名称。
4. 找出备注不为空且备注中包含“农场”的采集记录。
5. 挑战：用 `CASE` 把 `xp_level` 分成“新手”（<10）、“熟练”（10～24）、“老玩家”（>=25）。
