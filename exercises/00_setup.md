# 第 00 章练习：进入方块世界

1. 执行初始化脚本后，写出查询：分别统计 `players`、`gather_logs`、`trades` 的行数。
2. `gather_logs` 中一行代表什么？为什么“事件数”和“方块总数”不能用同一个聚合表达式？
3. 从 `information_schema.tables` 中只筛选 `blockworld` schema 下的表，并按表名排序。
4. 挑战：找出所有包含 `player_id` 字段的表。
